import React from 'react';
import ReactDOM from 'react-dom';
import './Front_End/tailwind.css'; // Adjust the path if necessary
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
